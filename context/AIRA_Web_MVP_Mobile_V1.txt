AIRA WEB MVP MOBILE V1
Target: D:\V3\Aira_web

PURPOSE
Upgrade the current web app into a cleaner, mobile-friendly MVP so the project can be tested properly on a phone.

This MVP should:
- work well on mobile first
- still work on desktop
- have a nicer landing page
- have a room/chat page
- have character cards
- keep the current backend structure
- stay simple enough to test quickly
- be ready for real AI API integration later

IMPORTANT
- Keep the existing project structure.
- Replace only the files listed below.
- Do NOT redesign the backend architecture.
- Do NOT add heavy frameworks.
- Use plain HTML, CSS, and JS for now.
- Make the UI responsive and easy to tap on mobile.

======================================================================
1) MVP GOALS
======================================================================

This is NOT the final product.
This is a testable UI shell for:

- multi-agent chat
- character vibe testing
- future image switching
- future relationship testing
- future AIRA presence testing

The goal is:
make it feel better than the current version, and easy to use on a phone.

======================================================================
2) UX STRUCTURE
======================================================================

Create 2 states inside the same frontend:

A) LANDING VIEW
- title / subtitle
- 3 character cards
- one "Enter Room" button
- clean premium look
- mobile-first stacked layout

B) ROOM VIEW
- hero image panel
- room title
- quick action chips
- chat log
- input bar fixed near bottom
- side debug info hidden on smaller screens

The user should be able to:
- open the site on mobile
- tap Enter Room
- chat immediately
- see state info in a lighter debug area
- use the app with one thumb comfortably

======================================================================
3) FILE TO REPLACE
======================================================================

FILE: client/index.html

----------------------------------------------------------------------
CODE
----------------------------------------------------------------------

<!DOCTYPE html>
<html lang="no">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover" />
  <title>AIRA Web</title>
  <link rel="stylesheet" href="./style.css" />
</head>
<body>
  <main class="app-shell">
    <section id="landingView" class="view active">
      <div class="landing-hero">
        <p class="eyebrow">AIRA ROOM</p>
        <h1>Et levende AI-rom</h1>
        <p class="subtitle">
          Snakk med flere karakterer, test kjemi, opplev skjulte spenninger,
          og oppdag noe merkelig i bakgrunnen over tid.
        </p>
      </div>

      <div class="card-grid">
        <article class="character-card">
          <div class="card-image card-lucy"></div>
          <div class="card-body">
            <h2>Lucy</h2>
            <p>Varm, intuitiv og vanskelig å lese helt.</p>
          </div>
        </article>

        <article class="character-card">
          <div class="card-image card-sam"></div>
          <div class="card-body">
            <h2>Sam</h2>
            <p>Holder avstand, men legger merke til alt.</p>
          </div>
        </article>

        <article class="character-card">
          <div class="card-image card-angie"></div>
          <div class="card-body">
            <h2>Angie</h2>
            <p>Leken og lett å snakke med, men skjuler mer enn hun viser.</p>
          </div>
        </article>
      </div>

      <div class="landing-actions">
        <button id="enterRoomBtn" class="primary-btn" type="button">Enter Room</button>
      </div>
    </section>

    <section id="roomView" class="view">
      <header class="room-header">
        <div>
          <p class="eyebrow">ROOM</p>
          <h1>Living Room</h1>
          <p class="subtitle">Flere karakterer, én scene, og noe som følger med.</p>
        </div>
        <button id="backBtn" class="ghost-btn" type="button">Back</button>
      </header>

      <section class="room-layout">
        <div class="main-column">
          <div class="hero-panel">
            <div id="heroImage" class="hero-image">
              <div class="hero-overlay">
                <div>
                  <p class="hero-kicker">Current Scene</p>
                  <h2 id="heroTitle">Late evening conversation</h2>
                  <p id="heroSubtitle">Subtle tension. No obvious threat.</p>
                </div>
              </div>
            </div>
          </div>

          <div class="chip-row">
            <button class="chip-btn" data-prompt="truth or dare">Truth or Dare</button>
            <button class="chip-btn" data-prompt="let's talk honestly">Be honest</button>
            <button class="chip-btn" data-prompt="i want to get closer to lucy">Closer to Lucy</button>
            <button class="chip-btn" data-prompt="sam, what do you really think">Ask Sam</button>
          </div>

          <div id="log" class="chat-log"></div>

          <form id="form" class="chat-bar" autocomplete="off">
            <input id="input" type="text" placeholder="Skriv noe..." />
            <button type="submit" class="send-btn">Send</button>
          </form>
        </div>

        <aside class="side-column">
          <div class="panel">
            <h3>Engine State</h3>
            <pre id="stateBox">Loading state...</pre>
          </div>

          <div class="panel">
            <h3>Observer Issues</h3>
            <pre id="issueBox">No issues yet.</pre>
          </div>

          <div class="panel">
            <h3>Patch Suggestions</h3>
            <pre id="patchBox">No patches yet.</pre>
          </div>
        </aside>
      </section>
    </section>
  </main>

  <script type="module" src="./app.js"></script>
</body>
</html>

======================================================================
4) FILE TO REPLACE
======================================================================

FILE: client/style.css

----------------------------------------------------------------------
CODE
----------------------------------------------------------------------

* {
  box-sizing: border-box;
}

:root {
  --bg: #0b1020;
  --panel: rgba(18, 25, 44, 0.9);
  --panel-2: rgba(25, 34, 58, 0.92);
  --line: rgba(255, 255, 255, 0.08);
  --text: #f8fafc;
  --muted: #aeb7c7;
  --accent: #8b5cf6;
  --accent-2: #22c55e;
  --danger: #f97316;
  --shadow: 0 16px 50px rgba(0, 0, 0, 0.28);
  --radius: 20px;
}

html, body {
  margin: 0;
  padding: 0;
  background:
    radial-gradient(circle at top, rgba(139, 92, 246, 0.18), transparent 35%),
    linear-gradient(180deg, #0a0f1d, #0c1222 45%, #090d19 100%);
  color: var(--text);
  font-family: Arial, sans-serif;
  min-height: 100%;
}

body {
  min-height: 100vh;
  padding:
    env(safe-area-inset-top, 0px)
    0
    env(safe-area-inset-bottom, 0px)
    0;
}

button,
input {
  font: inherit;
}

.app-shell {
  width: 100%;
  max-width: 1280px;
  margin: 0 auto;
  padding: 16px;
  padding-bottom: calc(16px + env(safe-area-inset-bottom, 0px));
}

.view {
  display: none;
}

.view.active {
  display: block;
}

.eyebrow {
  margin: 0 0 10px;
  color: #c4b5fd;
  letter-spacing: 0.2em;
  text-transform: uppercase;
  font-size: 12px;
  font-weight: 700;
}

.subtitle {
  margin: 0;
  color: var(--muted);
  line-height: 1.5;
}

.landing-hero {
  padding: 12px 4px 20px;
}

.landing-hero h1,
.room-header h1 {
  margin: 0 0 10px;
  font-size: clamp(28px, 7vw, 48px);
  line-height: 1.05;
}

.card-grid {
  display: grid;
  grid-template-columns: 1fr;
  gap: 14px;
  margin: 18px 0 18px;
}

.character-card {
  background: var(--panel);
  border: 1px solid var(--line);
  border-radius: 24px;
  overflow: hidden;
  box-shadow: var(--shadow);
}

.card-image {
  height: 220px;
  background-size: cover;
  background-position: center;
}

.card-lucy {
  background:
    linear-gradient(180deg, rgba(0,0,0,0.05), rgba(0,0,0,0.35)),
    radial-gradient(circle at 30% 20%, rgba(244, 114, 182, 0.45), transparent 35%),
    linear-gradient(135deg, #3b2f5f, #151c33);
}

.card-sam {
  background:
    linear-gradient(180deg, rgba(0,0,0,0.05), rgba(0,0,0,0.35)),
    radial-gradient(circle at 70% 18%, rgba(96, 165, 250, 0.35), transparent 30%),
    linear-gradient(135deg, #223149, #121a2a);
}

.card-angie {
  background:
    linear-gradient(180deg, rgba(0,0,0,0.05), rgba(0,0,0,0.35)),
    radial-gradient(circle at 35% 20%, rgba(250, 204, 21, 0.26), transparent 30%),
    linear-gradient(135deg, #3a2d1f, #181821);
}

.card-body {
  padding: 16px;
}

.card-body h2 {
  margin: 0 0 6px;
  font-size: 22px;
}

.card-body p {
  margin: 0;
  color: var(--muted);
  line-height: 1.45;
}

.landing-actions {
  display: flex;
  justify-content: center;
  margin-top: 18px;
}

.primary-btn,
.ghost-btn,
.send-btn,
.chip-btn {
  border: none;
  border-radius: 999px;
  padding: 13px 18px;
  cursor: pointer;
  transition: transform 0.15s ease, opacity 0.15s ease;
}

.primary-btn:active,
.ghost-btn:active,
.send-btn:active,
.chip-btn:active {
  transform: scale(0.98);
}

.primary-btn {
  background: linear-gradient(135deg, #8b5cf6, #a78bfa);
  color: white;
  font-weight: 700;
  min-width: 160px;
}

.ghost-btn {
  background: rgba(255,255,255,0.06);
  color: white;
  border: 1px solid var(--line);
}

.room-header {
  display: flex;
  gap: 14px;
  justify-content: space-between;
  align-items: flex-start;
  margin-bottom: 16px;
}

.room-layout {
  display: grid;
  grid-template-columns: 1fr;
  gap: 16px;
}

.main-column {
  min-width: 0;
}

.hero-panel {
  margin-bottom: 14px;
}

.hero-image {
  position: relative;
  min-height: 280px;
  border-radius: 24px;
  overflow: hidden;
  border: 1px solid var(--line);
  box-shadow: var(--shadow);
  background:
    linear-gradient(180deg, rgba(0,0,0,0.05), rgba(0,0,0,0.45)),
    radial-gradient(circle at 70% 25%, rgba(255,255,255,0.12), transparent 28%),
    linear-gradient(135deg, #1d2742, #10182b 55%, #0b1120);
}

.hero-overlay {
  position: absolute;
  inset: 0;
  display: flex;
  align-items: end;
  padding: 18px;
  background: linear-gradient(180deg, transparent 25%, rgba(0,0,0,0.45));
}

.hero-kicker {
  margin: 0 0 8px;
  color: #ddd6fe;
  font-size: 12px;
  text-transform: uppercase;
  letter-spacing: 0.16em;
}

.hero-overlay h2 {
  margin: 0 0 8px;
  font-size: clamp(22px, 5vw, 34px);
}

.hero-overlay p {
  margin: 0;
  color: #d1d5db;
}

.chip-row {
  display: flex;
  gap: 10px;
  overflow-x: auto;
  padding-bottom: 4px;
  margin-bottom: 14px;
  scrollbar-width: none;
}

.chip-row::-webkit-scrollbar {
  display: none;
}

.chip-btn {
  white-space: nowrap;
  background: rgba(255,255,255,0.08);
  color: white;
  border: 1px solid var(--line);
}

.chat-log {
  background: var(--panel);
  border: 1px solid var(--line);
  border-radius: 22px;
  min-height: 340px;
  max-height: 52vh;
  overflow-y: auto;
  padding: 14px;
  box-shadow: var(--shadow);
}

.msg {
  margin-bottom: 12px;
  padding: 12px 14px;
  border-radius: 16px;
  line-height: 1.45;
  white-space: pre-wrap;
  word-break: break-word;
}

.msg.user {
  background: #1d4ed8;
}

.msg.ai {
  background: rgba(255,255,255,0.08);
}

.msg.thought {
  background: rgba(255,255,255,0.06);
  color: #d1d5db;
  font-style: italic;
}

.msg.gm {
  background: rgba(139, 92, 246, 0.32);
}

.msg.system {
  background: rgba(249, 115, 22, 0.26);
}

.chat-bar {
  position: sticky;
  bottom: 0;
  display: flex;
  gap: 10px;
  margin-top: 14px;
  padding-top: 10px;
  padding-bottom: calc(4px + env(safe-area-inset-bottom, 0px));
  background: linear-gradient(180deg, rgba(11,16,32,0), rgba(11,16,32,0.86) 20%, rgba(11,16,32,1));
}

.chat-bar input {
  flex: 1;
  min-height: 50px;
  border-radius: 16px;
  border: 1px solid var(--line);
  background: rgba(13, 18, 32, 0.95);
  color: white;
  padding: 0 14px;
}

.send-btn {
  background: linear-gradient(135deg, #22c55e, #4ade80);
  color: #052e16;
  font-weight: 700;
  min-width: 88px;
}

.side-column {
  display: none;
  gap: 14px;
}

.panel {
  background: var(--panel-2);
  border: 1px solid var(--line);
  border-radius: 20px;
  padding: 14px;
  box-shadow: var(--shadow);
  margin-bottom: 14px;
}

.panel h3 {
  margin: 0 0 10px;
}

.panel pre {
  margin: 0;
  white-space: pre-wrap;
  word-break: break-word;
  color: #d5dbe7;
  font-size: 13px;
  line-height: 1.45;
}

@media (min-width: 760px) {
  .app-shell {
    padding: 22px;
  }

  .card-grid {
    grid-template-columns: repeat(3, 1fr);
  }

  .chat-log {
    min-height: 420px;
  }
}

@media (min-width: 1024px) {
  .room-layout {
    grid-template-columns: minmax(0, 1.4fr) 360px;
    align-items: start;
  }

  .side-column {
    display: block;
  }

  .chat-log {
    max-height: 56vh;
  }
}

======================================================================
5) FILE TO REPLACE
======================================================================

FILE: client/app.js

----------------------------------------------------------------------
CODE
----------------------------------------------------------------------

const landingView = document.getElementById('landingView');
const roomView = document.getElementById('roomView');
const enterRoomBtn = document.getElementById('enterRoomBtn');
const backBtn = document.getElementById('backBtn');

const form = document.getElementById('form');
const input = document.getElementById('input');
const log = document.getElementById('log');
const stateBox = document.getElementById('stateBox');
const issueBox = document.getElementById('issueBox');
const patchBox = document.getElementById('patchBox');
const heroTitle = document.getElementById('heroTitle');
const heroSubtitle = document.getElementById('heroSubtitle');
const chipButtons = document.querySelectorAll('.chip-btn');

function showLanding() {
  landingView.classList.add('active');
  roomView.classList.remove('active');
}

function showRoom() {
  landingView.classList.remove('active');
  roomView.classList.add('active');
  input.focus();
}

enterRoomBtn?.addEventListener('click', showRoom);
backBtn?.addEventListener('click', showLanding);

function appendMessage(text, kind = 'system') {
  const el = document.createElement('div');
  el.className = `msg ${kind}`;
  el.textContent = text;
  log.appendChild(el);
  log.scrollTop = log.scrollHeight;
}

function renderState(state) {
  stateBox.textContent = JSON.stringify(state, null, 2);

  const tension = state?.tension ?? 0;
  if (tension > 0.7) {
    heroTitle.textContent = 'The room feels different';
    heroSubtitle.textContent = 'Tension is high. Something feels slightly off.';
  } else if (tension > 0.4) {
    heroTitle.textContent = 'Charged atmosphere';
    heroSubtitle.textContent = 'The scene is still calm on the surface, but not fully.';
  } else {
    heroTitle.textContent = 'Late evening conversation';
    heroSubtitle.textContent = 'Subtle tension. No obvious threat.';
  }
}

function renderIssues(issues) {
  if (!issues || !issues.length) {
    issueBox.textContent = 'No issues yet.';
    return;
  }

  issueBox.textContent = issues
    .map((issue, index) => `${index + 1}. [${issue.type}] ${issue.message}`)
    .join('\n');
}

function renderPatches(patches) {
  if (!patches || !patches.length) {
    patchBox.textContent = 'No patches yet.';
    return;
  }

  patchBox.textContent = patches
    .map((patch, index) => `${index + 1}. ${patch.type}\n${patch.suggestion}`)
    .join('\n\n');
}

async function refreshState() {
  try {
    const res = await fetch('/api/ai/state');
    const data = await res.json();

    if (data.ok) {
      renderState(data.state);
    }
  } catch (error) {
    console.error('State refresh failed:', error);
  }
}

async function sendPrompt(text) {
  appendMessage(`YOU: ${text}`, 'user');

  try {
    const res = await fetch('/api/ai/run', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ input: text })
    });

    const data = await res.json();

    if (!data.ok) {
      appendMessage(`SYSTEM: ${data.error || 'Unknown error'}`, 'system');
      return;
    }

    const result = data.result || {};
    const responses = result.responses || [];

    for (const response of responses) {
      if (response.spoken) {
        appendMessage(`${response.agent}: ${response.spoken}`, 'ai');
      }

      if (response.showThought && response.thought) {
        setTimeout(() => {
          appendMessage(`${response.agent} thought: ${response.thought}`, 'thought');
        }, response.thoughtDelay || 350);
      }
    }

    if (result.gmLine) {
      appendMessage(result.gmLine, 'gm');
    }

    renderState(data.state);
    renderIssues(data.issues);
    renderPatches(data.patches);
  } catch (error) {
    appendMessage(`SYSTEM: ${error.message}`, 'system');
  }
}

form.addEventListener('submit', async (event) => {
  event.preventDefault();

  const text = input.value.trim();
  if (!text) return;

  input.value = '';
  await sendPrompt(text);
});

chipButtons.forEach((button) => {
  button.addEventListener('click', async () => {
    const text = button.dataset.prompt?.trim();
    if (!text) return;
    await sendPrompt(text);
  });
});

refreshState();

======================================================================
6) OPTIONAL NEXT STEP
======================================================================

After this mobile MVP works, the next best upgrade is:

A) Real AI API integration
- add .env support
- server-side API key only
- call LLM from backend

B) Visual State System
- switch image/title/subtitle based on state
- later support subtle AIRA presence in background

======================================================================
7) EXACT PROMPT FOR CLAUDE
======================================================================

Implement this exact mobile-friendly AIRA Web MVP in my current project.

Requirements:
- replace client/index.html exactly as provided
- replace client/style.css exactly as provided
- replace client/app.js exactly as provided
- keep the current backend routes
- keep the current engine integration
- do not add frameworks
- make sure the site works well on mobile
- after implementation, verify npm run dev starts cleanly

======================================================================
8) TEST CHECKLIST
======================================================================

After implementation, test on phone:
- landing page loads
- Enter Room button works
- chat input is easy to tap
- send button works
- chip buttons work
- room layout feels clean on mobile
- desktop still works
- no syntax errors

END OF FILE
